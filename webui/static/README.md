This directory will hold static assets (CSS/JS) for the future admin
web UI. Out of scope for this issue (see NG-1 on REA-85) -- the current
placeholder server (loop/webui.py) serves inline HTML directly and does
not read from this directory yet.
